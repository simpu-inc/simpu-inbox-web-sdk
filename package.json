{
  "name": "simpu-list-ui-sdk-root",
  "private": true,
  "scripts": {
    "build": "pnpm run -r build",
    "dev": "pnpm run --parallel -r dev",
    "clean": "rm -rf node_modules **/node_modules **/**/node_modules"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
